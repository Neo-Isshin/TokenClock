# TokenClock 四版本一致性契约

> 进行“四版功能一致”“跨平台对齐”或共享核心重构前，必须先阅读本文。
> 本文是刻意保留的平台差异清单；未登记的差异默认视为待修复问题。

## 适用版本

本文中的“四版本”指：

- macOS Glass（`main`）
- macOS Normal（`normal`）
- Windows Normal（`windows-port`）
- Linux Normal（`normal`）

首次登记版本：v1.5.1；适用基线始终以各分支最新 HEAD 为准。后续版本若改变行为或边界，必须同步更新本文。

## “功能一致”的默认含义

除下文登记的例外外，四版本必须提供一致的用户能力和数据语义。平台原生实现可以不同，但不得因此缺少功能、改变统计口径或产生不同结果。

必须保持一致的共享行为包括：

- Provider catalog、工具识别、模型归一化、用量与消息统计
- cache、cost、quota、history 和百分比的计算口径
- 价格数据、在线刷新与本地回退逻辑
- 设置项的含义、默认值、保存和重启后持久化
- `/api/usage`、`/api/history` 等公开 API 的字段与语义
- 四档表盘尺寸、详情展开、刷新、菜单、设置和订阅额度等用户能力
- 用户可见文案、语言切换及版本号

允许采用平台原生实现、但交互结果必须等价的项目包括窗口拖动、置顶、开机启动、文件选择器、右键菜单和安装/升级流程。

## 已登记的刻意差异

### PARITY-001：Text Color 快捷按钮的作用范围

| 版本 | 预期行为 |
| --- | --- |
| macOS Normal | 只切换详情面板文字颜色，不改变表盘文字颜色 |
| Windows Normal | 只切换详情面板文字颜色，不改变表盘文字颜色 |
| Linux Normal | 只切换详情面板文字颜色，不改变表盘文字颜色 |
| macOS Glass | 同时切换表盘与详情面板文字颜色，以适应透明表盘背后的明暗背景 |

原因：Normal 表盘具有稳定、不透明的自身背景，表盘文字不会因桌面背景而失去对比度；Glass 会直接受到桌面背景影响，需要快捷联动。

边界：

- 不得为了“四版一致”让三个 Normal 版本的快捷按钮修改表盘颜色。
- 不得为了“四版一致”取消 Glass 的表盘与详情面板联动。
- 右键菜单中的 `Dial Text Color` 和 `Detail Panel Text Color` 是独立的精细设置，必须继续保留；用户手动选择的颜色优先于快捷预设。
- 三个 Normal 版本应通过回归测试保证快捷预设不会改写表盘颜色。当前相关测试位于 `ClockInteractionTests` 和 `WindowsVisualBehaviorTests`。

### PARITY-002：Normal 与 Glass 的表盘覆盖层位置

| 版本 | 预期行为 |
| --- | --- |
| macOS Normal / Windows Normal / Linux Normal | 左侧工具标识和右侧热力/速率标识向表盘中心收拢，避开 9 点与 3 点数字 |
| macOS Glass | 保留 Glass 自己的材质与覆盖层布局，不套用 Normal 的固定偏移量 |

原因：Glass 与 Normal 的表盘材质、边缘留白和视觉重心不同。这里要求视觉结果协调，不要求使用完全相同的坐标常量。

边界：不得把 Normal 的坐标机械复制到 Glass；也不得以 Glass 为基准把 Normal 的标识重新移到数字上方。

### PARITY-003：Glass 专属外观能力

macOS Glass 可以拥有 Normal 不具备的透明材质、Glass Backing、Liquid Glass Refraction、Dial Tint 等外观控制。它们是产品版本差异，不属于 Normal 的缺失功能。

共同规则：`Dial Text Color` 与 `Detail Panel Text Color` 均为右键菜单一级条目，不再放入 `Dial Appearance` 子菜单。Glass 的专属外观条目同样可以直接位于一级菜单。

### PARITY-004：Small 表盘与详情面板尺寸

四版本的 Small 只缩小表盘；展开后的详情面板必须使用 Medium 的内容宽度（当前为 320 pt/px 级布局），避免按钮和文字被裁切。Medium、Large、Extra Large 的详情尺寸保持各自既有规则。

这是四版本共同规范，不是平台差异。不得把详情面板重新绑定到 Small 表盘宽度。当前 macOS 回归测试为 `testSmallDetailPanelUsesMediumWidthWithoutChangingOtherSizes`；Windows/Linux 需在尺寸交互烟测中覆盖。

### PARITY-005：平台原生系统集成与路径

以下内容允许实现不同，但用户能力与最终结果必须等价：

- macOS 使用 AppKit/SwiftUI，Windows 使用 Win32，Linux 使用 GTK/X11 对应实现。
- 开机启动、置顶、拖动、文件浏览和系统菜单使用各平台原生机制。
- Provider 默认路径必须按平台分别维护。Windows 的 `%APPDATA%`、`%LOCALAPPDATA%` 和用户目录，Linux 的 XDG/家目录，以及 macOS 的 Library/家目录不得互相套用。
- Windows、Linux 和 macOS 安装器及 one-liner 可以不同，但必须支持全新安装、覆盖升级、版本检查和可预测的安装位置。
- Emoji 可以使用平台字体或本地回退图形；含义、位置和可辨识度必须一致，不要求字形像素完全相同。

这些差异不能成为缺少 Provider、统计字段、设置项或交互入口的理由。

### PARITY-006：平台原生收起入口

macOS Normal 和 macOS Glass 提供菜单栏状态项；左键点击可隐藏全部 TokenClock 窗口，再次点击恢复原有窗口和状态。Windows Normal 通过表盘右键菜单的 Hide/Show 与系统托盘左键恢复提供等价能力。隐藏只影响可见界面，不暂停用量扫描、天气刷新、通知或本地 API。

Linux 当前没有对应入口，这属于已登记的平台系统集成差异。不得为了“四版功能完全一致”强行复制相同图标或系统 API，也不得因此改动共享统计核心；已提供入口的平台必须保持隐藏/恢复的用户可观察结果一致。

## 变更规则

新增或修改刻意差异时，必须同时完成：

1. 在本文增加或更新一个 `PARITY-XXX` 条目。
2. 写明适用版本、用户可观察行为、设计原因和禁止误改的边界。
3. 尽可能增加自动化回归测试；无法自动化的视觉差异必须列入对应平台的人工验收清单。
4. 检查共享核心的改动不会覆盖平台专属策略。
5. 在至少一台对应平台实机上验证，不以代码存在或成功编译代替交互验证。

若产品决定取消某项差异，应在四版本完成迁移和验证后删除对应条目，而不是先删除文档约束。

## 对 Agent 和维护者的执行约定

收到“四版完全一致”“同步所有平台”之类任务时：

- 先把本文登记项从机械对齐范围中排除，再验证它们仍符合各自预期。
- 对未登记的行为差异进行修复，或在确认其确有产品必要后先补充本文。
- 不得仅因实现文件不同就判定功能不一致；应比较用户可观察行为与数据结果。
- 不得仅因共享核心相同就判定功能一致；仍需验证各平台入口、渲染、路径、持久化和安装升级。
